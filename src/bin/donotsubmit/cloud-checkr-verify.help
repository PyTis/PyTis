(B)0[?1049h[1;46r[m[4l[46;1H[?1049l[?1l>[?1049h[1;46r[m[4l[H[J[46;1H[?1049l[?1l>Usage: cloud-checkr-verify [options]

cloud-checkr-verify
===================
IAM boto:
In this script we create a cloudcheckr group, IAM user, and Policy that 
provides access to cloudcheckr.

  -- OPTIONS:
  -h, --help            show this help message and exit

  -- RUNTIME ARGUMENTS:
    -b [PATH], --boto-config=[PATH]
    -c [PATH], --config-file=[PATH]
    -C [PATH], --credentials-file=[PATH]
    -p [PROFILE], --profile=[PROFILE]
                        If you have multiple profiles, you can configure
                        additional, named profiles by using the -p, --profile
                        option.


  -- CONFIGURATION SETTINGS:
    -a [STRING], --access-key-id=[STRING]
                        Normally, the AWS Access Key ID would be loaded from
                        the .aws/credentials file, however, you may choose to
                        manually override this option by providing the access
                        key id as an argument.

    -s [STRING], --secret-access-key=[STRING]
                        Normally, the AWS Access Key ID would be loaded from
                        the .aws/credentials file, however, you may choose to
                        manually override this option by providing the access
                        key id as an argument.


  -- DEBUG:
    -D, --debug         Enable debugging
    -V, --verbose       Be more Verbose (make lots of noise)
    -q, --quiet         be vewwy quiet (I'm hunting wabbits)
    -v, --version       Display Version


 cloud-checkr-verify run __doc__ help
	CloudTrail leverages Simple Storage Service (S3).  This program will talk to
	CloudTrail via it's API (built into AWS internal) to ...
	Steps:
		- create account
		- turn on cloud trail
		- ask cloud trail, please send logs to S3 Bucket in Master Computer
			Inncident Responce Team's (CIRT) account

	S3 Bucket that every account to point to.

	


Josh
aws --version: aws-cli/1.2.9 Python/3.4.0 Linux/3.13.0-48-generic
aws --version: aws-cli/1.2.9 Python/2.7.6 Linux/3.13.0-48-generic
boto:					 2.20.1

Bernie
aws --version: aws-cli/1.7.31 Python/2.7.9 Linux/3.14.35-28.38.amzn1.x86_64
boto:					 2.38.0
examples:	
	xxx

SEE ALSO:
	xxx

COPYRIGHT:
	Booz Allen Hamilton

AUTHOR:
	Josh Lee and Bernard Constantino

HISTORY:
	Original Author

VERSION:
	0
